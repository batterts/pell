<idea-plugin>
    <id>dev.pell</id>
    <name>pell</name>
    <vendor email="shaun.batterton@gmail.com" url="https://github.com/batterts">Shaun Batterton</vendor>
    <version>0.8.0</version>

    <description><![CDATA[
        <h2>Write PL/SQL like it's 2026</h2>

        <p><b>pell</b> is a statically-typed surface language that
        compiles to clean, idiomatic <b>Oracle PL/SQL</b> 19c / 23ai.
        Modern syntax, real type checking, ergonomic SQL — and the
        emitted PL/SQL is a deployable, human-readable, SHA-anchored
        artifact you commit, audit, and <code>git blame</code> like any
        other code.</p>

        <p>This plugin makes <b>IntelliJ IDEA</b>, <b>DataGrip</b>,
        <b>PyCharm</b>, and the rest of the JetBrains family a real
        editor for pell: split-view live preview, gutter run/build
        buttons, an embedded REPL, project scaffold, and full LSP-backed
        diagnostics / completion / go-to-definition.</p>

        <h3>One look at the lowering</h3>

<pre>
// src/hr/employees.pell
module hr.employees;

pub record Employee { id: number, name: text, level: number }
pub error NotFound { id: number }

pub fn promote(id: number) -> Result&lt;Employee, NotFound&gt; {
    let row: Employee = sql! {
        select id, name, level from employees where id = :id
    }.one()?;
    return Ok(Employee {
        id:    row.id,
        name:  row.name,
        level: row.level + 1,
    });
}
</pre>

        <p>…lowers to PL/SQL you'd write by hand:</p>

<pre>
CREATE OR REPLACE PACKAGE hr.employees AS
  TYPE t_employee IS RECORD (
    id    NUMBER,
    name  VARCHAR2(4000),
    level NUMBER
  );
  FUNCTION promote(p_id IN NUMBER) RETURN t_employee;
END employees;
/

CREATE OR REPLACE PACKAGE BODY hr.employees AS
  FUNCTION promote(p_id IN NUMBER) RETURN t_employee IS
    l_row t_employee;
    l_ret t_employee;
  BEGIN
    SELECT id, name, level
      INTO l_row
      FROM employees
     WHERE id = p_id;
    l_ret.id    := l_row.id;
    l_ret.name  := l_row.name;
    l_ret.level := (l_row.level + 1);
    RETURN l_ret;
  EXCEPTION
    WHEN NO_DATA_FOUND THEN
      RAISE pell_runtime.hr_employees_notfound;
  END promote;
END employees;
/
</pre>

        <h3>What you get inside the IDE</h3>
        <ul>
          <li><b>Side-by-side split editor.</b> Edit a <code>.pell</code>
              file, see the lowered PL/SQL update in real time on the
              right pane. Click a <code>pub fn</code> in the source →
              preview scrolls to the function body and highlights the
              line. ~400 ms debounce, error banner stays out of your way.</li>
          <li><b>Gutter actions.</b> Hammer icon on every
              <code>.pell</code> file: build, exec, parse, tokens, REPL.
              Play arrow on every <code>.sql</code> file: install on
              the connected Oracle. Works on IntelliJ Community — no
              Ultimate Database plugin required.</li>
          <li><b>Embedded REPL.</b> Notebook-style cells with variable
              persistence (auto-stringify, snapshot/replay across
              cells), <code>\load</code> a file into the session, full
              <code>p()</code> printing, <code>catalog::</code> data
              dictionary helpers, and live SQL execution.</li>
          <li><b>New Project wizard.</b> File → New → Project → pell
              scaffolds <code>src/hello.pell</code>, <code>tests/</code>,
              <code>plsql/</code> (committed!), a portable
              <code>./pell</code> shim, and a one-click Deploy run
              configuration.</li>
          <li><b>Tool window.</b> Project module tree with per-row
              build/deploy state, build-all, deploy-all, install
              <code>pell_runtime</code>, open REPL, all wired to the
              connected Oracle.</li>
          <li><b>LSP-backed intelligence.</b> Pell-LSP via LSP4IJ —
              real-time diagnostics, hover, completion (keywords +
              project identifiers + dotted-method menus), go-to-definition,
              outline, dotted-symbol completion (<code>catalog::columns_of</code>
              suggests the right pre-existing methods on the returned
              <code>list&lt;ColumnInfo&gt;</code>).</li>
          <li><b>Save-to-target-dir.</b> Save the .pell → the lowered
              SQL is rewritten to <code>plsql/&lt;name&gt;.sql</code> with
              <code>--reproducible</code> on. Commit both files;
              <code>git blame</code> shows you exactly how each
              deployment artifact evolved.</li>
        </ul>

        <h3>Why pell exists</h3>
        <p>PL/SQL the language has aged in places that bite — verbose
        package/spec/body duplication, awkward error handling, no real
        type checking across boundaries, terrible string interpolation,
        no module system. But Oracle the database is excellent, and
        the runtime semantics of PL/SQL are uniquely powerful. Pell
        keeps the database, replaces the surface:</p>
        <ul>
          <li><b>Generics &amp; sum types</b> (<code>Result&lt;T, E&gt;</code>,
              <code>Option&lt;T&gt;</code>, <code>list&lt;T&gt;</code>)
              that compile to the right PL/SQL collection / exception shapes.</li>
          <li><b>Real string interpolation</b> with auto-stringify for any
              type (<code>"got {n} tables"</code>) — number, date,
              timestamp, json, list, record, all handled.</li>
          <li><b>Embedded SQL</b> via <code>sql! { ... }</code> with
              <code>.one()</code> / <code>.collect()</code> / <code>.first()</code>
              terminators that drive the right INTO clause shape.</li>
          <li><b>JSON, regex, jq-style queries</b> as first-class —
              Oracle 23ai JSON_OBJECT_T under the hood with no
              <code>SELECT FROM dual</code> ceremony.</li>
          <li><b>The lowered PL/SQL is the deployment artifact</b>: SHA-
              anchored, byte-stable with <code>--reproducible</code>,
              meant to be committed and reviewed.</li>
        </ul>

        <h3>Learn more</h3>
        <ul>
          <li><b>Docs &amp; tutorial:</b>
              <a href="https://batterts.github.io/pell/">batterts.github.io/pell</a></li>
          <li><b>Examples</b> (30+ <code>.pell</code> files with their
              compiled output):
              <a href="https://github.com/batterts/pell/tree/main/compiler/examples">github.com/batterts/pell/tree/main/compiler/examples</a></li>
          <li><b>Source / contributing:</b>
              <a href="https://github.com/batterts/pell">github.com/batterts/pell</a></li>
          <li><b>Issues:</b>
              <a href="https://github.com/batterts/pell/issues">github.com/batterts/pell/issues</a></li>
        </ul>

        <p><i>Requires the pell CLI installed locally — the plugin
        shells out to it for compile, deploy, and REPL. Get it with
        <code>pip install -e .[repl]</code> from the repo, or open a
        new pell project in the wizard and it sets up a portable
        <code>./pell</code> shim for you.</i></p>

        <p><i>Keywords: PL/SQL, plsql, Oracle, Oracle database, Oracle
        23ai, SQL, transpiler, statically-typed, modern PL/SQL.</i></p>
    ]]></description>

    <change-notes><![CDATA[
        <h3>0.8.0 — SQL-transport debugging (outbound-only)</h3>
        <p><b>Debug with nothing but outbound database connections.</b>
        Set <code>PELL_DEBUG_TRANSPORT=sql</code> and the debugger drives
        Oracle's Probe API (DBMS_DEBUG) over two ordinary SQL sessions —
        no JDWP listener, no network ACE, no inbound path. If
        <code>pell exec</code> works through your tunnel, debugging now
        does too. Module breakpoints, stepping, locals, and stack all
        work; script-level breakpoints remain a jdwp-transport feature.
        Needs only the DEBUG CONNECT SESSION grant.</p>

        <h3>0.7.2 — debug through SSH tunnels</h3>
        <ul><li>Set <code>PELL_DEBUG_JDWP_PORT</code> to pin the IDE's
        JDWP listener port and <code>PELL_DEBUG_CALLBACK_HOST=127.0.0.1</code>
        to debug across a reverse SSH tunnel
        (<code>ssh -R 5005:localhost:5005 dbhost</code>) — see
        docs/DEBUGGER.md. Also new: <code>pell exec --trace</code>, a
        zero-privilege line-by-line execution trace for users who can't
        get DEBUG CONNECT SESSION granted.</li></ul>

        <h3>0.7.1 — debugger gutter fix</h3>
        <ul><li>The per-fn run/debug gutter icon now actually appears on
        <code>pub fn</code> (0.7.0 looked for <code>pub</code> inside the
        fn node; it lives on the wrapping item).</li></ul>

        <h3>0.7.0 — The pell debugger</h3>
        <p><b>Step through pell running inside Oracle.</b> Built on
        DBMS_DEBUG_JDWP (the SQL Developer mechanism): the database
        session connects back to the IDE and the IDE drives it.</p>
        <ul>
          <li><b>Breakpoints in .pell files</b> — gutter breakpoints map
              through compiler line markers to the deployed PL/SQL and
              snap to the nearest statement.</li>
          <li><b>Per-fn run/debug gutter actions</b> — every <code>pub fn</code>
              gets "Debug" which generates an editable stub
              (<code>.pell-debug/debug_&lt;module&gt;_&lt;fn&gt;.pell</code>)
              calling it with placeholder args. Step through the stub AND
              the fn.</li>
          <li><b>Falls through to PL/SQL</b> — frames outside pell source
              (runtime packages, hand-written PL/SQL) open the deployed
              ALL_SOURCE read-only and stepping continues.</li>
          <li>Variables view, stack, step over/into/out, console; uses
              <code>PELL_DB_URL</code>; needs DEBUG CONNECT SESSION + a
              JDWP network ACE (see docs/DEBUGGER.md).</li>
        </ul>

        <h3>0.6.1 — Extract Method works on loops and other nested-block statements</h3>
        <ul>
          <li><b>Extract a whole <code>for</code>/<code>if</code>/<code>match</code>
              statement.</b> Selecting a complete loop no longer fails with
              "selection must lie inside a single block" — the analyzer now
              finds the deepest block containing the whole selection instead
              of comparing the endpoints' blocks (the closing <code>}</code>
              belongs to the nested body).</li>
          <li><b>Loop variables are real declarations.</b> Extracting from
              inside a loop body captures <code>i</code> as a typed parameter
              (<code>number</code> for range loops); extracting the whole
              loop correctly keeps <code>i</code> internal.</li>
        </ul>

        <h3>0.6.0 — Module access domains + smarter refactorings</h3>
        <p><b>The module hierarchy is now an access-control boundary,
        enforced by Oracle itself.</b> Plus refactoring polish across
        the board.</p>
        <ul>
          <li><b>New module naming:</b> <code>module schema::a.b.c;</code>
              — the schema gets its own <code>::</code>-separated slot;
              the dotted path is unambiguously the package name
              (lowered to <code>schema.a_b_c</code>). No more
              "is the first segment a schema?" ambiguity.</li>
          <li><b>Access domains.</b> Non-root modules
              (<code>app.parsing</code>) emit
              <code>ACCESSIBLE BY (…)</code> listing every other package
              in their domain — Oracle rejects outside callers
              (PLS-00904), and pell rejects them earlier at compile time
              with a clear message. Root modules are the public
              interface. <code>@open</code> opts a module out.</li>
          <li><b>Move Symbol rewrites references</b> project-wide:
              qualified callers flip to the new module, bare uses in the
              old home get qualified, and missing imports are added.</li>
          <li><b>Extract Method infers param types</b> from the captured
              locals' declarations — no more <code>any</code> params
              that fail at deploy.</li>
          <li><b>Editor grammar parity:</b> <code>schema::</code> module
              declarations, dotted imports
              (<code>import app.parsing;</code>), and module-level
              annotations (<code>@stub</code>, <code>@open</code>) all
              parse without squiggles. Parser snapshot tests are now
              committed so grammar drift fails CI.</li>
        </ul>
        <h3>0.5.3 — Range expressions in the editor</h3>
        <p>The Grammar-Kit grammar now accepts range expressions
        (<code>for i in 1..=10</code>, <code>for i in 0..5</code>) and
        for-over-list-literal (<code>for x in [1, 2, 3]</code>) — the
        compiler already lowered these; the IDE parser had been
        red-squiggling them. Grammar + Python parser are back in sync.</p>
        <h3>0.5.2 — Refactorings actually work now</h3>
        <p><b>The PSI refactoring + navigation stack is real.</b> Every
        feature below is covered by headless platform tests that run the
        true IntelliJ pipelines (ReferencesSearch, rename, extract), so
        they don't silently rot.</p>
        <ul>
          <li><b>Find Usages works</b> — same-file and across files.
              The root cause was deep: Grammar-Kit's generated PSI
              elements aren't <code>ContributedReferenceHost</code>, so
              the reference contributor's references were silently
              dropped. References are now attached via
              <code>getReference()</code> mixins, which the platform
              consults unconditionally.</li>
          <li><b>All five refactorings work and appear in the Refactor
              menu:</b>
              <ul>
                <li><b>Rename</b> — declaration + every call site,
                    same-file and cross-file (needed
                    <code>ElementManipulator</code>s).</li>
                <li><b>Extract Method</b> — captures inputs as params
                    AND <i>outputs</i>: a local declared in the
                    selection but used afterwards is returned from the
                    new fn and rebound at the call site.</li>
                <li><b>Parameters → Record</b> — synthesizes the record,
                    rewrites the signature + body refs + call sites.</li>
                <li><b>Move Symbol</b> — moves the declaration (with its
                    leading <code>pub</code> / annotations) to another
                    module.</li>
                <li><b>Inline</b> and <b>Change Signature</b>.</li>
              </ul>
              Extract Method is now wired through a real
              <code>RefactoringSupportProvider</code>; the four action
              refactorings got the required
              <code>getActionUpdateThread()</code> so they show on
              modern IntelliJ.</li>
          <li><b>Go-to-definition</b> lands the cursor on the symbol
              name, not the leading <code>fn</code>/<code>record</code>
              keyword — so a follow-up Find Usages resolves correctly.</li>
          <li><b>Cross-package records</b> resolve from the REPL and
              <code>pell exec</code> the same as in a build, including
              dotted modules referred to by their short name.</li>
        </ul>
        <h3>0.4.1 — Parsed-lookup index (O(1) symbol resolution)</h3>
        <p><b>Performance + architecture cleanup.</b> Every symbol
        lookup (inspections, refactorings, completion, references)
        now goes through one project-level facade
        <code>PellSymbolService</code>, backed by an in-memory index
        (<code>PellProjectIndex</code>) refreshed by a VFS listener.
        Replaces the per-query PsiTreeUtil walk that landed in 0.4.0
        — same behaviour, O(1) lookups instead of O(project · file).</p>
        <ul>
          <li><b>New facade.</b> <code>PellSymbolService</code> is the
              single API every PSI consumer talks to. Returns rich
              <code>PellSymbolInfo</code> records (name, qualified
              name, kind, structured <code>PellType</code>, params,
              fields, source, PSI location). L3 will wire stdlib
              symbols (<code>catalog::*</code>, <code>logger::*</code>,
              <code>p</code>) into the same facade via a custom LSP
              method; no consumer change needed.</li>
          <li><b>Sealed type hierarchy.</b>
              <code>PellType.parse()</code> recognises every pell
              type form — primitives, <code>list&lt;T&gt;</code>,
              <code>Result&lt;T, E&gt;</code>, <code>T?</code>,
              <code>T | U</code>, named user types — and renders them
              back via <code>displayName()</code> for completion popup
              "type" labels and inspection messages.</li>
          <li><b>Cached index, refreshed by VFS listener.</b>
              <code>PellProjectIndex</code> indexes every project
              <code>.pell</code> file at first read and incrementally
              reindexes any file the IDE writes / creates / moves /
              deletes. Lives in memory; rebuilds on project open.
              Real disk-persistent <code>StubIndex</code> is the
              v0.5.0 upgrade if the in-memory rebuild ever feels
              heavy.</li>
          <li>Internal: <code>PellSymbolScanner</code> deleted —
              <code>PellProjectIndex</code> is the new home for
              project-wide lookup. <code>PellReferenceContributor</code>,
              <code>PellInlineAction</code>, <code>PellMoveSymbolAction</code>,
              <code>PellParamsToRecordHandler</code> all routed through
              the new index. No user-visible API or UI change.</li>
        </ul>
        <h3>0.4.0 — Real PSI parser + refactorings</h3>
        <p><b>Big release.</b> Pell gets a real IntelliJ-native PSI
        layer alongside the existing LSP. The LSP still drives
        diagnostics + hover; the PSI layer drives navigation,
        refactoring, structure view, and a growing set of inspections
        and intentions. The two are designed to co-exist.</p>
        <ul>
          <li><b>Grammar-Kit + JFlex parser.</b> Full port of the
              Python parser/lexer (1100+ lines of pell grammar) to
              Java/Kotlin so IntelliJ has a real AST to reason about,
              not just LSP-shaped diagnostics. Structure View
              (Cmd-7) now renders module / fn / record / error / type
              / enum / aggregate / seq children as real PSI elements.</li>
          <li><b>Cross-file navigation.</b> Cmd-Click on a pell symbol
              jumps to its declaration anywhere in the project.
              Cmd-Opt-O (Go To Symbol) lists every pub fn / record /
              type / enum across the project. Cmd-G (Find Usages)
              shows every call site of the symbol under the caret,
              traversing module boundaries.</li>
          <li><b>Rename refactoring.</b> Shift-F6 on any pell name —
              fn, record, error, type, enum, field, param, let, loop
              variable — propagates the rename through every
              reference in the project atomically. Names are validated
              against pell's keyword set before the rewrite commits.</li>
          <li><b>Extract Method.</b> Select a contiguous stmt run
              inside a fn body → Refactor → Extract Method. The new
              <code>pub fn</code> (or private fn) lands after the
              enclosing fn; captured locals become parameters; the
              selection becomes a call site. Won't extract selections
              that contain a <code>return</code> mixed with other
              stmts (refuses with a clear message).</li>
          <li><b>Parameters to Record.</b> Caret in a fn → Refactor
              This → Parameters to Record. Synthesizes a
              <code>pub record &lt;FnName&gt;Args</code>, rewrites the
              fn's signature to a single <code>args: FnNameArgs</code>
              parameter, rewrites parameter references in the body to
              <code>args.&lt;name&gt;</code>, and rewrites every call
              site to the struct-literal form.</li>
          <li><b>Move to Module / Inline / Change Signature.</b>
              Three more refactorings under Refactor This, working
              against any top-level pub declaration.</li>
          <li><b>Generate menu (Cmd-N).</b> Three pell-specific
              templates:
              <ul>
                <li><b>Field</b> — add a field to a record (with a
                    chooser when the caret isn't already inside one).</li>
                <li><b>CRUD for record</b> — pick a record, get
                    <code>select_*</code> / <code>insert_*</code> /
                    <code>update_*</code> / <code>delete_*</code> fns
                    with sql!{ } bodies, plus a matching
                    <code>pub error &lt;Record&gt;NotFound</code>.</li>
                <li><b>Aggregate skeleton</b> — full
                    state / step / merge / finish ODCI aggregate
                    body with sensible accumulator stubs.</li>
              </ul>
          </li>
          <li><b>Alt-Enter intentions.</b> On a bare expression-statement
              like <code>user_tables();</code>:
              <ul>
                <li><i>Introduce local variable</i> →
                    <code>let result = user_tables();</code></li>
                <li><i>Assign to local variable</i> →
                    <code>let result: list&lt;text&gt; = user_tables();</code></li>
                <li><i>Wrap in for-loop</i> →
                    <code>for row in user_tables() { /* TODO */ }</code></li>
              </ul>
          </li>
          <li><b>Inspection: discarded function return value.</b>
              Yellow squiggle on bare-call statements where the callee
              is a project fn with a non-Unit return type — the
              edit-time defense for the PLS-00221 pattern. Resolution
              is qualifier-aware (<code>logger::info(x);</code> only
              fires if a project <code>module logger;</code> defines
              <code>info</code> with a return type), and skips
              procedures (no <code>-&gt; T</code> clause), built-ins
              (<code>p()</code>, <code>dbms_output::*</code>,
              <code>catalog::*</code>), and member-access calls
              (<code>obj.method()</code> — type inference comes next
              release).</li>
          <li><b>Completion popup is richer.</b> Ctrl-Space now lists
              pell primitive types (<code>number</code>,
              <code>text</code>, <code>bool</code>, <code>date</code>,
              <code>timestamp</code>, <code>interval</code>,
              <code>bytes</code>, <code>json</code>, <code>Unit</code>,
              <code>bigtext</code>, <code>int</code>) first, then
              keywords, then project pub fns + records, then in-scope
              locals. Coexists with the LSP4IJ completion feed.</li>
        </ul>
        <p><b>Coexistence with LSP4IJ:</b> diagnostics, hover, and
        existing LSP-side completion continue to work as before.
        Phase L1+L2 of the
        <a href="https://github.com/batterts/pell/blob/main/intellij/LSP_PSI_UNIFICATION.md">LSP_PSI_UNIFICATION.md</a>
        plan will unify the two stacks via a single PellSymbolService
        facade backed by a StubIndex, replacing the current PsiTreeUtil
        walks with O(1) lookups.</p>
        <h3>0.3.13</h3>
        <ul>
          <li><b>One-click pell install + first-launch detection.</b>
              Project open now probes for a working <code>pell</code>
              CLI on <code>$PELL_HOME</code>, the project shim, and
              <code>$PATH</code>. If none is found, a friendly
              notification surfaces with an "Install pell" button
              that jumps to Settings → Tools → pell, where the CLI
              status row + an Install button live.</li>
          <li>The Install button runs
              <code>pip install --user "pell[repl]"</code> in a
              background task and notifies on success / failure.
              On macOS / Linux this leaves <code>pell</code> in
              <code>~/.local/bin</code> (Python user-base) — restart
              the IDE after install so the updated PATH is picked up.</li>
        </ul>
        <h3>0.3.12</h3>
        <ul>
          <li><b>Rewritten Marketplace description</b> — opens with a
              side-by-side `.pell` → emitted PL/SQL example so people
              searching <i>plsql</i> / <i>Oracle</i> see the lowering
              immediately. Links to docs, examples repo, and source.
              No behavior change in the IDE itself; the rest of the
              0.3.x line ships as-is.</li>
        </ul>
        <h3>0.3.11</h3>
        <ul>
          <li><b>"Run preview on PELL_DB_URL" can no longer fire on a
              broken preview.</b> Three layered defenses:
              <ol>
                <li>The error preview body is fully `-- `-prefixed,
                    so even if you shell <code>./pell sql</code> on
                    the temp file by hand, Oracle sees a comment
                    block — no ORA-00900 on path strings.</li>
                <li>The gutter Run icon is removed when the preview
                    is showing an error, not just when there's a
                    good compile.</li>
                <li>Defensive bail in <code>runOnPellDbUrl()</code>:
                    if the preview isn't currently showing valid
                    compiled PL/SQL, surface an IntelliJ notification
                    ("preview isn't a valid compile yet") instead of
                    writing garbage to a temp file.</li>
              </ol>
              Fixes the symptom of
              <code>[1/2] /Users/.../pell  FAIL — ORA-00900</code>
              that you'd see if you ran the gutter icon on a broken
              preview.</li>
        </ul>
        <h3>0.3.10</h3>
        <ul>
          <li><b>Caret-sync lands on the body, not the spec.</b> Click
              <code>pub fn greet(...)</code> in the source and the
              preview now scrolls to <code>FUNCTION greet IS / BEGIN</code>
              inside <code>PACKAGE BODY</code> — where the code lives —
              instead of the signature line in the spec. Search begins
              at the <code>CREATE OR REPLACE PACKAGE BODY</code> marker
              and matches the `IS`-prefixed body shape.</li>
          <li><b>Highlighted target line.</b> The line you jumped to
              is now painted with the IDE's caret-row background color
              so your eye finds it immediately. Only one line is
              highlighted at a time; the previous mark clears on the
              next nav (and when the preview re-compiles).</li>
        </ul>
        <h3>0.3.9</h3>
        <ul>
          <li><b>plsql/ everywhere.</b> The build-output dir is now
              `plsql/` by default (was `built/`) — the lowered SQL
              is the deployment artifact, meant to be committed and
              git-blamed, not hidden away as ephemeral build output.
              <ul>
                <li>New Project wizard scaffolds <code>plsql/.gitkeep</code>
                    instead of <code>built/</code>, and the .gitignore
                    no longer excludes it.</li>
                <li>Tool window's Build / Run SQL / Install runtime
                    buttons all honor Settings → Tools → pell →
                    "Target directory".</li>
                <li>Module-state scan looks at both the configured
                    target dir AND the legacy <code>built/</code>, so
                    pre-existing projects keep working until migrated.</li>
              </ul>
          </li>
          <li><b>New CLI: <code>pell migrate</code></b> — idempotent
              helper for existing projects. <code>pell migrate</code>
              renames <code>built/</code> → <code>plsql/</code> and
              drops the corresponding <code>.gitignore</code> entry.
              Dry-run with <code>--dry-run</code> first to preview.</li>
          <li><b><code>pell deploy</code> default output dir is now
              <code>plsql/</code>.</b> Override with <code>--out-dir</code>
              if you've kept the legacy name.</li>
        </ul>
        <h3>0.3.8</h3>
        <ul>
          <li><b>Save-to-target-dir wired up.</b> When you save a
              .pell file (Cmd-S), the plugin now runs
              <code>./pell build &lt;source&gt; -o
              &lt;targetDir&gt;/&lt;name&gt;.sql</code> and refreshes
              the VFS so the file pops into the project tree. The
              committed PL/SQL artifact stays in lockstep with the
              authored .pell. Target directory configured in
              Settings → Tools → pell (default <code>plsql</code>).
              Hook: VFS_CHANGES → VFileContentChangeEvent on the
              source file. Async — doesn't block the save callback.
              Failures are silent (the preview pane already shows
              compile errors).</li>
        </ul>
        <h3>0.3.7</h3>
        <ul>
          <li><b>Project settings (Settings → Tools → pell)</b> with
              one knob today: <b>Target directory</b> (where the
              lowered PL/SQL is written / read by <code>pell deploy</code>).
              Default <code>plsql</code>; you can pick <code>sql</code>,
              <code>built</code>, or anything else. Persisted per-project
              in <code>.idea/pell.xml</code>. Setting is in place; the
              IntelliJ flows that write output will pick it up in the
              next iteration.</li>
        </ul>
        <h3>0.3.6</h3>
        <ul>
          <li><b>Caret sync between source and preview.</b> Click on
              <code>pub fn greet(...)</code> in the .pell source and
              the preview scrolls to <code>FUNCTION greet</code>.
              Works for fn / record / type / error / aggregate / enum
              / sealed declarations. Name-based mapping (no source-map
              yet); good enough for most navigation.</li>
          <li><b>.pell gutter menu trimmed.</b> Removed <code>Run pell
              build</code> and <code>Run pell build → file</code> —
              the split-editor preview already shows the lowered
              PL/SQL live, so these were just noise. Menu is now:
              exec, exec (dry-run), Open REPL, parse (debug),
              tokens (debug).</li>
        </ul>
        <h3>0.3.5</h3>
        <ul>
          <li><b>The .pell gutter icon is now a hammer (compile), not a
              play arrow (run).</b> A .pell file's primary action is
              <i>build</i> — `pell build` compiles to PL/SQL — not
              execution. The play arrow is now exclusive to the
              preview pane's "Run on PELL_DB_URL" button (which
              actually does execute). The dropdown items in the
              .pell gutter are unchanged.</li>
        </ul>
        <h3>0.3.4</h3>
        <ul>
          <li><b>Run-on-PELL_DB_URL gutter button in the PL/SQL preview
              pane.</b> The split editor's right-hand preview now has a
              green-play gutter icon on line 1. Click it to install the
              currently-displayed PL/SQL against the connected database
              — no need to save the .pell, no need to open a terminal.
              The plugin writes the in-memory preview to a temp .sql,
              opens a terminal tab, and runs <code>./pell sql</code>
              with the output streaming inline. Reinstalls on every
              successful re-compile, so you always run the latest
              lowering.</li>
        </ul>
        <h3>0.3.3</h3>
        <ul>
          <li>Clears all Marketplace verifier warnings: replaces three
              deprecated / internal API usages with their stable
              successors.
              <ul>
                <li><code>TerminalToolWindowManager.createLocalShellWidget(...)</code>
                    → <code>createShellWidget(cwd, name, focus, defer)</code>
                    + <code>TerminalWidget.sendCommandToExecute(...)</code></li>
                <li><code>StartupManager.runAfterOpened(...)</code>
                    (internal) → scaffold directly in
                    <code>generateProject</code></li>
                <li><code>ProjectGeneratorPeer.getComponent()</code>
                    override (deprecated) removed; settings go through
                    <code>buildUI(SettingsStep)</code> only</li>
                <li><code>DirectoryProjectGenerator.getLogo()</code>
                    → <code>getIcon()</code></li>
                <li><code>SyntaxHighlighterFactory.LANGUAGE_FACTORY.forLanguage(...)</code>
                    → <code>SyntaxHighlighterFactory.getSyntaxHighlighter(lang, ...)</code></li>
                <li><code>LightVirtualFile</code> from <code>testFramework</code>
                    (internal) — no longer needed; SQL highlighter built
                    directly via the language-aware factory</li>
                <li><code>PsiAwareTextEditorProvider()</code> ctor
                    (internal) → <code>TextEditorProvider.getInstance()</code></li>
              </ul>
              No user-visible behavior change — same UI, same flows,
              but the binary is now forward-compatible with 2025.x and
              2026.x EAPs without verifier warnings.</li>
        </ul>
        <h3>0.3.2</h3>
        <ul>
          <li><b>Preview no longer flickers while you type.</b> When
              the source has incomplete syntax (mid-typing
              <code>pub fn foo(</code>), the PL/SQL preview now stays
              frozen on the last successful compile. A red banner
              above the preview surfaces the compile error — no more
              blanking out and re-rendering on every keystroke.</li>
        </ul>
        <h3>0.3.1</h3>
        <ul>
          <li>Plugin ID renamed from <code>dev.pell.intellij</code> to
              <code>dev.pell</code>. JetBrains Marketplace rejects IDs
              containing the word "intellij" — required for the first
              public submission. If you installed 0.3.0 manually,
              uninstall it before installing 0.3.1 (different ID
              means the IDE treats them as separate plugins).</li>
        </ul>
        <h3>0.3.0</h3>
        <ul>
          <li><b>Side-by-side pell ↔ PL/SQL split editor.</b>
              Opening any <code>.pell</code> file now shows the source
              on the left and a live PL/SQL preview on the right
              (Markdown-style). Recompiles debounced (~400ms) as you
              type. Toggle text-only / split / preview-only via the
              icons in the editor's top-right corner — same UI as the
              Markdown plugin. Preview gets SQL syntax highlighting
              when available. Compile errors render in the preview
              pane with header + stderr.</li>
        </ul>
        <h3>0.2.4</h3>
        <ul>
          <li><b>Build / install pell_runtime from the tool window.</b>
              Two new toolbar buttons:
              <ul>
                <li><b>Build pell_runtime.sql</b> — aggregates every
                    pell <code>error</code> declared in the project
                    into <code>built/pell_runtime.sql</code>.</li>
                <li><b>Install pell_runtime on PELL_DB_URL</b> — same
                    build, then installs the generated SQL against
                    the connected database. Fires the install only if
                    the build half exited 0.</li>
              </ul>
              Previously this only happened as a side-effect of
              <code>Deploy all</code> when at least one module
              referenced <code>pell_runtime.&lt;exception&gt;</code>.
              Standalone access makes the workflow explicit.</li>
        </ul>
        <h3>0.2.3</h3>
        <ul>
          <li>Tool window now falls back to <code>project.basePath</code>
              when the project has no modules / no content roots. Fixes
              the empty-tree case for brand-new IDEA projects created
              via the New Project wizard, where IDEA doesn't auto-create
              a module — the previous code iterated zero roots and
              found zero files even though the scaffold was on disk.</li>
        </ul>
        <h3>0.2.2</h3>
        <ul>
          <li>Tool window auto-refreshes when <code>.pell</code> or
              <code>.sql</code> files appear or disappear under the
              project's content roots. Fixes the empty-tree case in
              brand-new projects: the wizard's scaffold writes files
              <i>after</i> the tool window's initial scan, so the
              tree used to be empty until the user clicked Refresh.
              Now the VFS subscription catches the post-startup
              writes and the tree populates without a click.</li>
        </ul>
        <h3>0.2.1</h3>
        <ul>
          <li><b>New Project entry now actually shows up in IntelliJ IDEA.</b>
              0.2.0 used <code>&lt;directoryProjectGenerator&gt;</code>
              which only surfaces in PyCharm / WebStorm / RubyMine /
              DataGrip. IDEA's Generators list reads from
              <code>&lt;moduleBuilder&gt;</code> with a
              <code>GeneratorNewProjectWizardBuilderAdapter</code>
              (same surface Maven Archetype uses). Now registered both
              ways so the "pell" entry appears in every JetBrains IDE.</li>
        </ul>
        <h3>0.2.0</h3>
        <ul>
          <li><b>Tool window</b>: new "pell" tab on the right strip with a
              module tree (per-row build/deploy state), toolbar (Build,
              Build all, Run SQL, Deploy all, Open REPL, Open source,
              Open built SQL), and a streaming log panel.</li>
          <li><b>Open pell REPL</b> gutter action — opens IntelliJ's
              terminal and auto-<code>\\load</code>s the current
              <code>.pell</code> file into a fresh session.</li>
          <li><b>Run SQL gutter</b> on <code>.sql</code> files — uses
              the pell driver instead of Ultimate's Database plugin.</li>
          <li><b>New Project wizard</b>: File → New → Project → pell
              scaffolds a complete starter project with the
              <code>./pell</code> wrapper, <code>src/</code>,
              <code>built/</code>, and a Deploy all run config.</li>
          <li>Build-arg ordering fixed for <code>pell build -o</code>
              (file before flag, matches the CLI).</li>
        </ul>
        <h3>0.1.0</h3>
        <ul>
          <li>Initial run-configuration type and gutter-icon popup
              (exec / build / parse / tokens).</li>
          <li><code>.pell</code> file-type registration.</li>
          <li>LSP4IJ wiring for diagnostics / hover / outline.</li>
        </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.terminal</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Notification group for warnings the plugin surfaces as
             IntelliJ toasts (e.g. "preview isn't valid yet — fix
             the source first" on the Run-on-PELL_DB_URL gutter). -->
        <notificationGroup
            id="pell"
            displayType="BALLOON"
            isLogByDefault="true" />

        <!-- Register the .pell file type -->
        <fileType
            name="pell"
            language="pell"
            extensions="pell"
            implementationClass="dev.pell.intellij.PellFileType"
            fieldName="INSTANCE" />

        <!-- Language definition (Plain language; PSI is handled by LSP4IJ) -->
        <lang.parserDefinition
            language="pell"
            implementationClass="dev.pell.intellij.PellParserDefinition" />

        <!-- Run-configuration support — surfaces "pell" in the
             Run/Debug "+" menu and powers the gutter run icon. -->
        <configurationType
            implementation="dev.pell.intellij.run.PellRunConfigurationType" />
        <runConfigurationProducer
            implementation="dev.pell.intellij.run.PellRunConfigurationProducer" />
        <runLineMarkerContributor
            language="pell"
            implementationClass="dev.pell.intellij.run.PellRunLineMarkerContributor" />

        <!-- Debugger: gutter breakpoints in .pell files, a Debug
             executor for pell run configs (JDWP via DBMS_DEBUG_JDWP —
             the SQL Developer mechanism), and per-fn run/debug gutter
             actions that generate a calling stub. -->
        <xdebugger.breakpointType
            implementation="dev.pell.intellij.debugger.PellLineBreakpointType" />
        <programRunner
            implementation="dev.pell.intellij.debugger.PellDebugRunner" />
        <runLineMarkerContributor
            language="pell"
            implementationClass="dev.pell.intellij.debugger.PellDebugFnLineMarkerContributor" />

        <!-- Run-on-Oracle gutter icon for plain `.sql` files. Registered
             for both TEXT (Community fallback) and SQL (Ultimate's
             Database plugin owns the language there). The two
             registrations target DIFFERENT language ownerships of the
             same file extension — only one fires per file based on
             which language IntelliJ assigns to .sql. -->
        <runLineMarkerContributor
            language="TEXT"
            implementationClass="dev.pell.intellij.run.PellSqlRunLineMarkerContributor" />
        <runLineMarkerContributor
            language="SQL"
            implementationClass="dev.pell.intellij.run.PellSqlRunLineMarkerContributor" />

        <!-- Project settings page (Settings → Tools → pell).
             Lets the user choose the directory name where the lowered
             PL/SQL is written. Backed by the per-project
             PellSettings persistent state component. -->
        <projectConfigurable
            parentId="tools"
            id="dev.pell.settings"
            displayName="pell"
            instance="dev.pell.intellij.settings.PellSettingsConfigurable" />

        <!-- First-launch detection: on project open, probe for a working
             pell CLI. If missing, surface a friendly install nag with a
             button that jumps to Settings → Tools → pell. -->
        <postStartupActivity
            implementation="dev.pell.intellij.settings.PellFirstLaunchActivity" />

        <!-- Side-by-side pell ↔ PL/SQL split editor. Opens .pell files
             in a Markdown-style editor with the source on the left
             and a live PL/SQL preview on the right. Recompiles
             debounced (~400ms) as the user types. -->
        <fileEditorProvider
            implementation="dev.pell.intellij.preview.PellSplitEditorProvider" />

        <!-- Side-panel tool window (icon strip, "pell" tab). Shows
             module tree + status + build/deploy log. -->
        <toolWindow
            id="pell"
            secondary="false"
            anchor="right"
            icon="AllIcons.Actions.Compile"
            factoryClass="dev.pell.intellij.toolwindow.PellToolWindowFactory" />

        <!-- New Project wizard. Two registrations because different
             IntelliJ-family IDEs surface different surfaces:
               - <moduleBuilder> with a GeneratorNewProjectWizardBuilderAdapter
                 → IntelliJ IDEA's "Generators" section (Maven Archetype,
                   Spring Boot, ...). Same surface Maven Archetype uses.
               - <directoryProjectGenerator> → PyCharm / WebStorm /
                   RubyMine / DataGrip — their wizard reads from this EP.
             Same scaffolding logic; both paths land in PellProjectScaffold. -->
        <moduleBuilder
            builderClass="dev.pell.intellij.newproject.PellNewProjectWizard$PellNewProjectWizardBuilder" />
        <directoryProjectGenerator
            implementation="dev.pell.intellij.newproject.PellDirectoryProjectGenerator" />

        <!-- >>> PSI TRACK >>> -->
        <!-- Cross-file reference resolution: every qualified path like
             `hr::employees::Employee` carries a PsiReference back to the
             declaring PellNamedElement.

             NOTE: references are now attached via getReference() mixins
             (PellQualifiedExprMixin / PellTypeRefMixin), NOT this
             contributor. Grammar-Kit's ASTWrapperPsiElement is not a
             ContributedReferenceHost, so PsiReferenceContributor
             providers were silently dropped by SharedPsiElementImplUtil
             — which broke Find Usages and PSI Rename. The contributor
             registration is left out; the reference *classes*
             (PellQualifiedReference / PellTypeReference) still live in
             PellReferenceContributor.kt and are used by the mixins. -->
        <!--
        <psi.referenceContributor
            language="pell"
            implementation="dev.pell.intellij.psi.PellReferenceContributor" />
        -->

        <!-- Element manipulators — tell the platform how to rewrite a
             reference-bearing element's text. Rename's
             handleElementRename delegates here; without them it throws
             "No ElementManipulator instance registered". -->
        <lang.elementManipulator
            forClass="dev.pell.intellij.psi.impl.PellQualifiedExprMixin"
            implementationClass="dev.pell.intellij.psi.PellQualifiedExprManipulator" />
        <lang.elementManipulator
            forClass="dev.pell.intellij.psi.impl.PellTypeRefMixin"
            implementationClass="dev.pell.intellij.psi.PellTypeRefManipulator" />

        <!-- Find Usages (Cmd-G / Edit ▸ Find Usages). Re-enabled in
             Week 2 — disabling it knee-capped Rename's cross-file
             scope too, since IntelliJ's ReferencesSearch walks the
             WordsScanner registered here. Root-cause is being fixed
             upstream in PellFindUsagesProvider / PellQualifiedReference
             rather than papered over. -->
        <lang.findUsagesProvider
            language="pell"
            implementationClass="dev.pell.intellij.codeInsight.PellFindUsagesProvider" />


        <!-- Names validator — used by Rename (Phase 5) and by Inspections
             to reject names that would collide with pell keywords or
             aren't legal IDENTs. -->
        <lang.namesValidator
            language="pell"
            implementationClass="dev.pell.intellij.refactor.PellNamesValidator" />

        <!-- Go To Symbol (Cmd-Opt-O) — feeds every project-level
             PellNamedElement into the popup. -->
        <gotoSymbolContributor
            implementation="dev.pell.intellij.codeInsight.PellGotoSymbolContributor" />

        <!-- Rename (Shift-F6) processor — claims every PellNamedElement
             so the platform routes new-name validation through
             PellNamesValidator and references through PellReferenceContributor. -->
        <renamePsiElementProcessor
            implementation="dev.pell.intellij.refactor.PellRenameProcessor" />

        <!-- Refactoring support — surfaces Extract Method
             (Cmd-Alt-M / Refactor ▸ Extract Method…) and drives the
             "Refactor This" popup + in-place rename availability.
             Replaces the bogus <refactoring.extractMethod> EP, which
             doesn't exist and was silently ignored (so Extract Method
             never appeared in the menu). -->
        <lang.refactoringSupport
            language="pell"
            implementationClass="dev.pell.intellij.refactor.PellRefactoringSupportProvider" />

        <!-- PSI-backed autocomplete (Ctrl-Space). Adds primitive types,
             keywords, project pub fn / record names, and in-scope locals
             to the popup. Merges with LSP4IJ's completion feed. -->
        <completion.contributor
            language="pell"
            implementationClass="dev.pell.intellij.completion.PellCompletionContributor" />

        <!-- PellSymbolService project-level facade. Every inspection,
             refactoring, intention, and completion contributor routes
             symbol lookups through this service.
               L1: PSI-walk backed.
               L2: PellProjectIndex backed (O(1) lookup, refreshed by a
                   VFS listener; see PellProjectIndex.kt for trade-offs
                   vs. real disk-persistent StubIndex). -->
        <projectService
            serviceInterface="dev.pell.intellij.symbols.PellSymbolService"
            serviceImplementation="dev.pell.intellij.symbols.PellSymbolServiceImpl" />

        <!-- The index itself. Eagerly initialized on first read.
             Refreshed per-file by PellProjectIndexInvalidator's VFS
             listener (registered as a postStartupActivity below). -->
        <projectService
            serviceImplementation="dev.pell.intellij.symbols.index.PellProjectIndex" />

        <!-- Wires the VFS listener into the project lifecycle. -->
        <postStartupActivity
            implementation="dev.pell.intellij.symbols.index.PellProjectIndexInvalidator" />

        <!-- Yellow-squiggle inspection: bare expression-statement whose
             expression is a function call whose return value is discarded.
             Discarded returns produce PLS-00221 at deploy time, so we
             catch them at edit time. Quick-fixes: assign to local, wrap
             in for-loop. -->
        <localInspection
            language="pell"
            shortName="PellDiscardedReturn"
            displayName="Discarded function return value"
            groupName="Pell"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.pell.intellij.inspections.PellDiscardedReturnInspection" />

        <!-- Alt-Enter intention actions. Surfaced when the caret is on a
             bare expression-statement (e.g. `user_tables();`). -->
        <intentionAction>
            <language>pell</language>
            <className>dev.pell.intellij.intentions.PellIntroduceVariableIntention</className>
            <category>Pell</category>
        </intentionAction>
        <intentionAction>
            <language>pell</language>
            <className>dev.pell.intellij.intentions.PellAssignToLocalVariableIntention</className>
            <category>Pell</category>
        </intentionAction>
        <intentionAction>
            <language>pell</language>
            <className>dev.pell.intellij.intentions.PellWrapInForLoopIntention</className>
            <category>Pell</category>
        </intentionAction>
        <!-- <<< PSI TRACK <<< -->
    </extensions>

    <!-- >>> PSI TRACK >>> -->
    <actions>
        <!-- Parameters to Record — extracts a fn's parameter list into a
             new pub record and rewrites every call site to the struct-literal
             form. Available under Refactor This (Ctrl-T / Ctrl-Alt-Shift-T). -->
        <action id="pell.ParametersToRecord"
                class="dev.pell.intellij.refactor.paramsToRecord.PellParamsToRecordAction"
                text="Parameters to Record"
                description="Extract a fn's parameter list into a new pub record type">
            <add-to-group group-id="RefactoringMenu" anchor="last" />
        </action>

        <!-- Move Symbol to Module — F6 on a pub fn / record / type opens a
             chooser, moves the declaration to the chosen module, and updates
             every cross-module reference. Phase 8. -->
        <action id="pell.MoveSymbolToModule"
                class="dev.pell.intellij.refactor.move.PellMoveSymbolAction"
                text="Move to Module..."
                description="Move a pub declaration to another module"
                use-shortcut-of="Move">
            <add-to-group group-id="RefactoringMenu" anchor="last" />
        </action>

        <!-- Inline — opposite of Extract Method. Substitutes every call
             site with the fn body and deletes the declaration. Phase 8. -->
        <action id="pell.InlineSymbol"
                class="dev.pell.intellij.refactor.inline.PellInlineAction"
                text="Inline..."
                description="Replace call sites with the fn body and delete the declaration"
                use-shortcut-of="Inline">
            <add-to-group group-id="RefactoringMenu" anchor="last" />
        </action>

        <!-- Change Signature — modify params (add/remove/reorder/retype) and
             propagate type-safe rewrites to every call site. Phase 8. -->
        <action id="pell.ChangeSignature"
                class="dev.pell.intellij.refactor.signature.PellChangeSignatureAction"
                text="Change Signature..."
                description="Modify a fn's signature and propagate the change to every call site"
                use-shortcut-of="ChangeSignature">
            <add-to-group group-id="RefactoringMenu" anchor="last" />
        </action>

        <!-- Generate menu items (Cmd-N / Alt-Insert) — pell-specific
             templates, not generic getters/setters which don't apply to
             a value-semantics language with auto-derived equality. -->
        <action id="pell.GenerateField"
                class="dev.pell.intellij.generate.PellGenerateFieldAction"
                text="Field"
                description="Add a field to a record">
            <add-to-group group-id="GenerateGroup" anchor="last" />
        </action>
        <action id="pell.GenerateCrud"
                class="dev.pell.intellij.generate.PellGenerateCrudAction"
                text="CRUD for record"
                description="Generate insert / select / update / delete fns from a record's fields">
            <add-to-group group-id="GenerateGroup" anchor="last" />
        </action>
        <action id="pell.GenerateAggregate"
                class="dev.pell.intellij.generate.PellGenerateAggregateAction"
                text="Aggregate skeleton"
                description="Insert a state / step / merge / finish aggregate body">
            <add-to-group group-id="GenerateGroup" anchor="last" />
        </action>
    </actions>
    <!-- <<< PSI TRACK <<< -->
</idea-plugin>
