module hello;

import std::logger;
import collections_linked_list;

pub fn greet(name: text) -> text {
  logger::info(name);
  logger::err("test");

    let a: Summary = Summary {
        count: 0,
        total: 0,
        contains_zero: false,
    };

    let result = collections_linked_list::from_list([1,2,3,4,5,6,7,8]);
    return "{a.count} {result}";

}
